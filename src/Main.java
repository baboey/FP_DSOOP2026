import graph.DS;

public class Main {
    public void main() {
        DS graph = new DS();
        graph.addVertex(1);
        graph.addVertex(2);
        graph.addVertex(3);
        graph.addVertex(4);
        graph.printVertex();
    }
}
